class Loc {
  
  int x;
  int y;
  
  // ----------------------------------------------------------
  // instantiate a location object at given x and y coordinates
  // ----------------------------------------------------------
  Loc( int tempX, int tempY ) {
    x = tempX;
    y = tempY;
  }
}

